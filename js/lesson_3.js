/* var user = {
    name: 'Vladimir',
    photo: {
        url: "www.x3-kto.com",
        sizes: [64, 128, 256]
    }
}

console.log(user);
console.log(user.photo.sizes[1]);

var arr = [];
for(var i = 0; i < 3; i++) {
    arr[i] = prompt('Введите ' + (i + 1) + ' число:');
}

for(var i = 0; i < 3; i++) {
    console.log(arr[i]);
} */
/* var num = 1,
    result = 0;

while (num != 0 && true) {
    num = prompt("Введите число");
    if (num == 0 || String(Number(num)) == "NaN") {
        break;
    }
    result = result + Number(num);
}
alert(result); */
